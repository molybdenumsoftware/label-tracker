query BranchContainsQuery($owner: String!, $name: String!, $branch: String!, $commit: String!) {
  repository(owner: $owner, name: $name) {
    ref(qualifiedName: $branch) {
      compare(headRef: $commit) {
        status
      }
    }
  }
}

query PullsQuery($owner: String!, $name: String!, $after: String, $batch: Int!) {
  rateLimit {
    limit
    cost
    remaining
    resetAt
  }
  repository(owner: $owner, name: $name) {
    pullRequests(first: $batch, after: $after, orderBy: { direction: ASC, field: UPDATED_AT }) {
      pageInfo {
        endCursor
        hasNextPage
      }
      edges {
        node {
          number
          baseRefName
          mergeCommit {
            oid
          }
        }
      }
    }
  }
}
